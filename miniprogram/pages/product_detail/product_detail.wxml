<!-- 轮播图 -->
<view class="banner">
  <swiper class="banner_1" autoplay="true" circular="true" interval="5000" indicator-dots="true">
    <swiper-item wx:for="{{product_src}}" wx:key="index">
      <image src="{{item}}" style="width:750rpx;height:750rpx"></image>
    </swiper-item>
  </swiper>
</view>

<!-- 产品信息 -->
  <view class="goods_price">￥{{product_price}}</view>
  <view class="goods_name_row">
    <view class="goods_name">{{product_name}}</view>
  </view>

  <view class="product">
    <view class="product_detail">初处理步骤</view>
    <view wx:for="{{product_detail}}" wx:key="_id" class="product_1">
      <view>{{item}}</view>
    </view>
  </view>

  <!-- <block wx:for="{{fenlei}}" wx:key="_id">
      <view class="fenlei_left_1" bindtap="get_product_fenlei" data-name="{{item}}">
        <image src="{{item.img}}" style="width:50rpx;height:50rpx"></image>
        <view>{{item}}</view>
      </view>
    </block> -->
    
  <view class="line"></view>
  <!-- 商品详情 -->
  <view class="product">
    <view class="product_detail">配料信息</view>
    <view wx:for="{{product_contain}}" wx:key="_id" class="product_1">
      <view>{{item}}</view>
    </view>
  </view>
  
 

  <view class="product_delete">
    <view>请输入需要删减的辅料（主要成分不给予删减）</view>
  </view>
    <view style="padding: 15rpx;">
      <textarea placeholder="请输入需要添加或删减的辅料" bindblur="search" ></textarea>
    </view>


    <!-- <checkbox-group bindchange="checkboxChange">
          <label class="weui-cell weui-check__label" wx:for="{{items}}" wx:key="{{item.value}}">
            <view class="weui-cell__hd">
              <checkbox value="{{item.value}}" checked="{{item.checked}}"/>
            </view>
            <view class="weui-cell__bd">{{item.name}}</view>
          </label>
        </checkbox-group> -->
    <!-- <checkbox-group class="checkboxgroup" bindchange="checkboxChange">
      <label class="checkbox" wx:for="{{product_contain}}">
        <checkbox value="{{item}}" checked="{{item.checked}}" />{{item}}
      </label>
    </checkbox-group> -->
  

  <view style="align-self: center;height:150rpx">已到底</view>


  <!-- 按钮 -->
  <view class="pay">
    <view class="pay_1" bindtap="into_shopping_cart">
      <image src="cloud://cloud1-6gtiz48ybf23c5c5.636c-cloud1-6gtiz48ybf23c5c5-1306427100/购物车.png"
        style="width:80rpx;height:80rpx">
      </image>
      <view>加入购物车</view>
    </view>
    <view class="pay_2" bindtap="buy">立即购买</view>
  </view>